global:
  scrape_interval: 5s # How often to check the app (very fast for testing)

scrape_configs:
  - job_name: 'observability-core'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8083']